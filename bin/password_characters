#!/usr/bin/env ruby
password = ARGV[0]
characters_indices = ARGV[1]

indices = characters_indices.split(',').map { |n| n.to_i }

password_characters = indices.map { |index| password[index - 1] }
puts password_characters.join ' '